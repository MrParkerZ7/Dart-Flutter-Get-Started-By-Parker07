name: flutter_17_cached_images
description: A new Flutter application.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.0
  cached_network_image: ^0.3.0 #Request this.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
